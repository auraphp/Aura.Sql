language: php
env:
  - DB=sqlite
  - DB=mysql
  - DB=postgres
php:
  - 5.4
before_script:
  - "mysql -e 'DROP DATABASE IF EXISTS test; create database test;'"
  - "psql -c 'DROP DATABASE IF EXISTS test; create database test;' -U postgres"
  - cd tests
script: phpunit
