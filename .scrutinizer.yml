build:
    image: default-bionic
    environment:
        php: 8.4.0
    packages:
        - openssl=1.1.*
    nodes:
        analysis:            
            tests:
                override:
                    - php-scrutinizer-run
filter:
    paths: ["src/*"]
