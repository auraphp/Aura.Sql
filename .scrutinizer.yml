build:
    image: default-bionic
    environment:
        php: 8.4.0
    nodes:
        analysis:
            dependencies:
                before:
                    - sudo apt-get update
                    - sudo apt-get install -y openssl libssl-dev
            tests:
                override:
                    - php-scrutinizer-run
filter:
    paths: ["src/*"]
